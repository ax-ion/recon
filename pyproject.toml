# pyproject.toml
[project]
name = "recon"
version = "1.0.0"
description = "Reconnaissance on your codebase — docs, directory trees, and project maps for AI"
requires-python = ">=3.10"
dependencies = ["pyperclip"]

[project.scripts]
recon = "main:main"
